# =============================================================================
# Digivixo Voice AI Platform — environment variables
#
# Every key listed as ACTIVE is referenced somewhere in the code right now.
# Keys under "UPCOMING" are defined in docs/02-manual-admin-steps (section 8)
# and will move up to ACTIVE as the prompts that reference them are built.
# Placeholder values only — real credentials live in .env (never committed).
# =============================================================================

# --- ACTIVE (referenced in code as of Prompt 1) ------------------------------

# Neon Postgres — use the POOLED connection string, not the direct one
# (Next.js serverless functions exhaust direct connections under load).
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# --- UPCOMING (not yet referenced in code; added in later prompts) -----------

# REDIS_URL=your_upstash_redis_url                    # Prompt 3
# RETELL_API_KEY=your_retell_api_key                  # Prompt 3
# RETELL_SIGNING_KEY=your_retell_signing_key          # Prompt 3
# RESEND_API_KEY=your_resend_api_key                  # Prompt 2.5 / 5
# R2_ACCOUNT_ID=your_cloudflare_account_id            # Prompt 4
# R2_ACCESS_KEY_ID=your_r2_access_key_id              # Prompt 4
# R2_SECRET_ACCESS_KEY=your_r2_secret_access_key      # Prompt 4
# R2_BUCKET_NAME=your_r2_bucket_name                  # Prompt 4
# POLAR_ACCESS_TOKEN=your_polar_api_access_token      # Prompt 2.5 / 6
# POLAR_WEBHOOK_SECRET=your_polar_webhook_signing_secret  # Prompt 6
# POLAR_ENVIRONMENT=sandbox_or_production             # Prompt 2.5 / 6
